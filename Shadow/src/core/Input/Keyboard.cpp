#include "Keyboard.h"

namespace ShadowEngine {

	
}


/*

// while there is some thing in the queue
while (!keyEvents.Empty()) {
// grab the event at the front of the queue
KeyEvent* ev = keyEvents.Peek()->data;

// if it has not been handled
if (!ev->Handled()) {
// it needs to be handled
//SH_DEBUGGER_INFO("KEY EVENT BEING HANDLED");

ev->Handled(true);
keyEvents.Dequeue();
}

}

return true;
*/